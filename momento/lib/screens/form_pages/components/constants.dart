import 'package:flutter/material.dart';

const kFormTextFont = TextStyle(fontSize: 16.0);

const kFormInputPadding = EdgeInsets.symmetric(
  vertical: 10.0,
  horizontal: 12,
);

const kFormInputDecoration = InputDecoration(
  filled: true,
  fillColor: Colors.white,
  border: OutlineInputBorder(),
  contentPadding: kFormInputPadding,
);

const kFormTitlePadding = EdgeInsets.symmetric(vertical: 3.0);

const kFormTitleStyle = TextStyle(fontSize: 16);

const kFormFieldPadding = EdgeInsets.only(bottom: 8.0);
